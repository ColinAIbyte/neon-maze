# 首页摘要 A 方案与图鉴遮挡修复

本页保留当时的本地验收记录；后续已由 `4d3d6aa` 保存并纳入本次发布，当前发布范围见 [优化验收](OPTIMIZATION-2026-09-05.md)。下文的“未发布”描述的是测试发生时状态，不代表当前部署状态。

## 范围

- 摘要三行统一 15px；冠军金色 / 700，其余淡蓝 / 500。
- 名次、昵称、分数三列，所有数字使用等宽数位，分数右对齐。
- 长昵称视觉省略，完整文本仍保留在 DOM 与 title；分数不省略。
- 保留原有高度不超过 650px 时只显示冠军的行为；隐藏规则限定直接子行，不隐藏行内数字。
- 旧接口降级记录也使用分列排版，仍不伪造名次。
- 图鉴与玩法说明共享不透明全屏文档布局、系统安全区和内部滚动。
- 图鉴层级 130，覆盖开始页及常驻语言 / 榜单入口（120 / 121），低于完整排行榜（300）。
- 不隐藏或改写底层开始页状态，保留暂停 / 恢复规则；后续补齐图鉴入口的键盘隔离与焦点返回。
- 完整排行榜领奖台、玩法说明、关于、暂停、结算的既有行为均保留。
- 无新依赖、路由、数据库改动或请求；原有未提交的最近战绩改动保留。

## 自动检查

新增 `node 源码/工具/test_ui_polish.mjs`：13 组通过（含后续入口修复）。
覆盖中英文 0–3 条、同分名次、长昵称、八位分数、旧接口、空 / 断网状态、图鉴关闭返回与暂停状态、文档页互斥、样式和短屏选择器。

以下 13 套既有检查通过，未更改其预期：

- `test_hall_integration`、`test_hall_view`、`test_recent_scores`
- `test_build_consistency`、`test_english_build`、`test_hall_build`
- `test_help_close`、`test_about`、`test_progress`
- `test_touch_targets`、`test_web_gesture`、`test_markup`
- `源码/微信小游戏版/工具/smoke.mjs`

已运行 `build_web.mjs`、`build_weapp.mjs`，中英文、网站镜像与微信核心一致；`git diff --check` 通过。

## 本机浏览器检查

真实生成页面，隔离示例网络与存储；不是线上服务验收，也不代表手机真机触摸验收。

- 中文摘要：375×812、320×700、320×568、667×375、1280×900 均无页面横向溢出。
- 可见三行实测均为 15px，字重 700 / 500 / 500，分数右边缘完全一致。
- 8 字中文昵称 / 8 个宽英文字符正常省略，八位分数完整保留，三列不相碰。
- 短屏保留冠军整行，名次、昵称、分数均未被误隐藏。
- 英文 320×700：同样字号、字重、对齐与无溢出。
- 图鉴 375×812：背景 rgb(10, 6, 20)，position fixed，区域为整屏，顶部入口被遮挡，关闭按钮可见。
- 图鉴 667×320：正文可视高 193px / 内容高 296px，实际滚动 103px 后到达底部，关闭按钮可见。
- 关闭图鉴后回到原开始页；英文图鉴 320px 无横向溢出、关闭按钮可见。

预览服务运行时可打开 http://127.0.0.1:8878/ 。页面明确标注示例数据，所有外部 fetch 被隔离，不读写正式榜。

## 补充：图鉴入口图标与点击行为

- 按用户反馈，将通用猫头鹰 emoji 替换为游戏内亮青色敌人“闪闪”的头像；复用现有 `enemy-thumb`、同一张图集及瞳孔样式，不增加素材或依赖。
- 点击区域为 44×44；保留“对手图鉴 / ENEMY CODEX”可访问名称，并标注弹窗关系与展开状态。
- 修复前隔离检查复现：图鉴打开后按 Enter，状态从 ready 变成 playing，图鉴仍覆盖在上面。
- 修复后图鉴进入页面判断，入口 Enter / 空格交给原生按钮；打开后焦点移入图鉴，Tab 在正文和关闭按钮之间循环，关闭后恢复入口焦点。
- 中文 390×844：头像及瞳孔显示正常，无猫头鹰 emoji；点击、Enter、Tab / Shift-Tab、Esc、关闭按钮均实测通过。
- 英文 320×700：本地图集路径正确；Enter 打开不启动底层游戏，图鉴无横向溢出，关闭按钮可见，Enter 关闭后返回入口。
- 同源网页 / 微信核心已重建；额外通过 `test_keyboard`、`test_enemy_profiles`，既有图鉴 / 关于 / 玩法说明 / 云榜回归及微信冒烟检查通过。
- 上述检查仍为本机隔离验收，未发布，未访问正式数据。
